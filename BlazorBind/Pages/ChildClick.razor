@page "/childclick"

<h3>子コンポーネントのイベントを受け取る</h3>

<BlazorBind.Components.BmiCalc OnResultChanged="@ChangeResult" ></BlazorBind.Components.BmiCalc>
<div>
    BMI結果：@Bmi
</div>

@code {
    public double Bmi { get; set; }
    private void ChangeResult(double result)
    {
        this.Bmi = result;
    }

}

